package com.example.inventory.controller;

@RestController
@RequestMapping("/api/inventory")
public class InventoryController {
    // Define endpoints for adding, updating, deleting, and retrieving items
    // ...
}
